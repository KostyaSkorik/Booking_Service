# Coworking Booking Service 🚀

Микросервис для бронирования рабочих мест в коворкинге. Проект построен на принципах **чистой архитектуры (Clean Architecture)** с использованием **гексагонального подхода (Ports and Adapters)**.

---

### ✨ Основные Возможности

Проект предоставляет комплексную систему бронирования со следующими ключевыми функциями:

*   **Модульная Монолитная Архитектура:** Структурирована на модули `domain`, `service`, `persistence` и `presentation` для обеспечения четкого разделения ответственности и модульности, следуя принципам чистой/гексагональной архитектуры.
*   **Полное Управление Бронированиями:** Управление созданием, чтением, обновлением и удалением бронирований, включая валидацию для предотвращения пересечений по времени.
*   **Управление Коворкинг-пространствами:** Функциональность для определения и управления доступными для бронирования коворкинг-пространствами.
*   **Управление Пользователями и Аутентификация OAuth2:** Управление данными пользователей и защита конечных точек API с использованием стандартных механизмов аутентификации OAuth2.
*   **Надежное Хранение Данных:** Использует Spring Data JPA с PostgreSQL в качестве основной реляционной базы данных для надежного и эффективного хранения.
*   **Миграция БД с Flyway:** Автоматическое управление схемой базы данных через структурированные миграции с помощью Flyway, обеспечивая плавную эволюцию БД.
*   **Событийное Взаимодействие через Kafka:** Реализует событийные паттерны с использованием Apache Kafka для асинхронных уведомлений о бронированиях и взаимодействия между сервисами.
*   **RESTful API:** Предоставляет набор RESTful конечных точек для плавного взаимодействия с функциями бронирования, управления коворкингами и пользователями.
*   **Централизованная Обработка Ошибок:** Включает `GlobalExceptionHandler` для предоставления согласованных и стандартизированных ответов об ошибках по всему API.
*   **Автоматическое Преобразование Данных:** Использует MapStruct для эффективного преобразования между DTO и сущностями, уменьшая количество шаблонного кода.
*   **Сокращение Шаблонного Кода с Lombok:** Использует Lombok для уменьшения шаблонного кода в моделях и DTO.
*   **Контейнеризированная Среда Разработки:** Объединяет серверную часть, базу данных и другие компоненты с помощью `docker-compose` для удобства настройки и согласованности среды разработки.
*   **Интеграция с Фронтендом (Заглушка):** Директория `frontend` содержит `nginx.conf` и `index.html`, демонстрируя возможность интеграции с отдельным фронтенд-приложением.

---

## 🛠 Технологический стек

| Категория                     | Технология                                                                          | Примечания                                                                                                                                  |
|:------------------------------| :---------------------------------------------------------------------------------- |:--------------------------------------------------------------------------------------------------------------------------------------------|
| **Язык Программирования**     | Java 17                                                                             | Основной язык для серверной логики.                                                                                                         |
| **Фреймворк**                 | Spring Boot, Spring WebMVC, Spring Data JPA, Spring Security OAuth2 Client          | Основа для построения серверного приложения.                                                                                                |
| **Система Сборки**            | Apache Maven                                                                        | Система управления проектами, используемая для всех Java-модулей.                                                                           |
| **База Данных**               | PostgreSQL                                                                          | Реляционная база данных для хранения данных.                                                                                                |
| **Миграции Базы Данных**      | Flyway                                                                              | Управляет и автоматизирует миграции схемы базы данных.                                                                                      |
| **Обмен Сообщениями/Очереди** | Apache Kafka                                                                        | Распределенная потоковая платформа для событийного взаимодействия, в частности для уведомлений о бронированиях(3 брокера, KRaft mode)                               |
| **Утилиты**                   | Project Lombok, MapStruct, dotenv-java                                              | Lombok сокращает шаблонный код Java; MapStruct для эффективного преобразования объектов; dotenv-java для загрузки переменных окружения.     |
| **Контейнеризация**           | Docker, Docker Compose                                                              | Для упаковки и запуска приложения и его зависимостей (PostgreSQL, Kafka) в контейнерах, упрощая развертывание и настройку среды разработки. |
| **Веб-Сервер**                | Nginx (для Фронтенда)                                                               | Используется для обслуживания статических фронтенд-ресурсов и потенциально в качестве обратного прокси.                                     |
| **Хранилище медиа-файлов**    | MinIO                                                              | S3-совместимое хранилище для медиа-файлов. Используется для отображения картинок коворкингов                                                |
---

## 🏗 Архитектура

Проект использует четкую модульную архитектуру, организованную в несколько модулей Maven, каждый со своей ответственностью:

*   **`domain`**: Содержит ядро бизнес-модели, сущности, исключения и интерфейсы (порты), определяющие операции предметной области без зависимостей от деталей реализации. Это сердце бизнес-логики приложения.
*   **`service`**: Реализует бизнес-логику высокого уровня. Этот модуль выступает в роли слоя приложения, оркестрируя операции предметной области и вызывая порты для сохранения данных.
*   **`persistence`**: Содержит реализации портов из модуля `domain` с использованием Spring Data JPA и PostgreSQL. Этот модуль отвечает за взаимодействие с базой данных (сущности JPA, репозитории) и также управляет взаимодействием с Kafka. Здесь же используется Flyway для миграций схемы.
*   **`presentation`**: Слой API, содержащий RESTful-контроллеры, DTO (Data Transfer Objects) и конфигурацию безопасности (OAuth2). Отвечает за предоставление функциональности приложения через HTTP-конечные точки и обработку входящих запросов.
*   **`frontend`**: Эта директория содержит статические фронтенд-файлы (`index.html`) и конфигурацию Nginx (`nginx.conf`), что указывает на возможность наличия отдельного фронтенд-приложения.

`docker-compose.yaml` оркестрирует все необходимые компоненты (серверное приложение, база данных PostgreSQL, Kafka) для предоставления самодостаточной среды разработки.

---

## 🚀 Быстрый старт (Docker Compose)

В проект включена полная конфигурация инфраструктуры. Вам не нужно устанавливать Kafka или Postgres локально.

### 1. Запуск окружения

Убедитесь, что у вас установлен Docker и запущен Docker Desktop. Выполните команду в корне проекта:

```bash
docker-compose up -d

```
Либо просто запустите приложение, так как установлен spring-boot-docker-compose, который сам запустит docker-compose

### 2. Доступные сервисы

| Сервис | Адрес | Описание |
| --- | --- | --- |
| **Backend API** | `http://localhost:8080` | Основное приложение |
| **PostgreSQL** | `localhost:5430` | База данных (DB: booking_test_db) |
| **Kafka UI** | `http://localhost:8090` | Панель управления Kafka (Просмотр топиков) |
| **MinIO Console** | `http://localhost:9001` | Панель управления S3 (admin/password) |
| **Frontend** | `http://localhost:3000` | Nginx с вашим фронтендом |

---

## 📨 Работа с Kafka

Проект использует кластер из 3-х брокеров для обеспечения высокой доступности.

### Проверка событий бронирования

Чтобы прослушать сообщения из топика `create_booking_topic` через консоль:

```bash
docker exec -it kafka-1 /opt/bitnami/kafka/bin/kafka-console-consumer.sh \
  --bootstrap-server localhost:9092 \
  --topic create_booking_topic \
  --from-beginning

```

**Ключевые настройки продюсера:**

* `linger.ms=0`: Мгновенная отправка для низкой задержки.
* `enable.idempotence=true`: Гарантия защиты от дублей при сетевых сбоях.
* `request.timeout.ms=5000`: Таймаут ожидания подтверждения от брокера.

---